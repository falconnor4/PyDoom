In order to activate python3 virtual enviornment. run the following in the terminal:

$ source bin/activate

then, in order to actually run the pydoom script. run the doom.py script using

python3 Main.py